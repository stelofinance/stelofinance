<nav aria-label="Menu"
     class="fixed bottom-4 left-1/2 -translate-x-1/2 z-20 flex bg-neutral-800 rounded-full ring ring-neutral-700
     text-neutral-400 lg:text-xl"
>
	<a href="/app"
	   class="flex items-center px-2.5 py-1 rounded-full {{if eq .ActivePage `home`}}text-white bg-anakiwa-700{{end}}"
	>
		{{template "icons/home"}}
		{{if eq .ActivePage `home`}}
		Home
		{{end}}
	</a>
	<a href="/app/accounts"
	   class="flex items-center px-2.5 py-1 rounded-full {{if eq .ActivePage `accounts`}}text-white bg-anakiwa-700{{end}}"
	>
		{{template "icons/wallet"}}
		{{if eq .ActivePage `accounts`}}
		Accounts
		{{end}}
	</a>
	<a href="/app/transfers"
	   class="flex items-center px-2.5 py-1 rounded-full {{if eq .ActivePage `transfers`}}text-white bg-anakiwa-700{{end}}"
	>
		{{template "icons/transfer"}}
		{{if eq .ActivePage `transfers`}}
		Home
		{{end}}
	</a>
	{{/*<a href="/app/market"
	   class="flex items-center px-2.5 py-1 rounded-full {{if eq .ActivePage `market`}}text-white bg-anakiwa-700{{end}}"
	>
		{{template "icons/market-candles" ``}}
		{{if eq .ActivePage `market`}}
		Home
		{{end}}
	</a>*/}}
</nav>
