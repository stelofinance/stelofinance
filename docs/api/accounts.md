# Accounts

## Routes
All routes are prefixed with `/accounts/{account_id}`, so if the docs say `/transfers` it's `/accounts/{account_id}/transfers`.
The parameter `{account_id}` should be replaced with your account's id.
