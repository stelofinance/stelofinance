[package]
name = "stelofinance"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
spacetimedb = { version = "2.7.*", features = ["unstable"] }
log = "0.4"
serde_json = "1"
http = "1"
